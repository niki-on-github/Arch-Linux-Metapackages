# Maintainer: arch <arch[at]local>
pkgname=personal-xfce
pkgver=0.0.1
pkgrel=1 # release of this package version
pkgdesc="Personal xfce setup"
url="http://gitea.local:3000/r/arch-packages"
arch=('any')
license=('MIT')
source ../resolve_dependencies.sh
depends=($(resolve_dependencies "xfce.depends"))
depends+=( 'personal-xorg' )
install=xfce.install

package() {
    return 0
}

