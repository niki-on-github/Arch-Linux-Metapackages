# Maintainer: arch <arch[at]local>
pkgname=personal-vbox-guest
pkgver=0.0.1
pkgrel=1 # release of this package version
pkgdesc="Personal VirtualBox guest setup"
url="http://gitea.local:3000/r/arch-packages"
arch=('any')
license=('MIT')
source ../resolve_dependencies.sh
depends=($(resolve_dependencies "vbox-guest.depends"))
install=vbox-guest.install

package() {
    return 0
}

