# Maintainer: arch <arch[at]local>
pkgname=personal-base-wm
pkgver=0.0.1
pkgrel=1 # release of this package version
pkgdesc="Personal base WM setup"
url="http://gitea.local:3000/root/Arch-Packages"
arch=('any')
license=('MIT')
groups=('desktop-pc')
source ../resolve_dependencies.sh
depends=($(resolve_dependencies "base-wm.depends"))
depends+=( 'personal-base' )
install=base-wm.install

package() {
    return 0
}

