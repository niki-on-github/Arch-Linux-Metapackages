# Maintainer: arch <arch[at]local>
pkgname=personal-lxc
pkgver=0.0.1
pkgrel=1 # release of this package version
pkgdesc="Personal LXC setup"
url="http://gitea.local:3000/root/Arch-Packages"
arch=('any')
license=('MIT')
source ../resolve_dependencies.sh
depends=($(resolve_dependencies "lxc.depends"))
install=lxc.install

package() {
    return 0
}

