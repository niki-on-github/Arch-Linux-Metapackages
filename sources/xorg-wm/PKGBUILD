# Maintainer: arch <arch[at]local>
pkgname=personal-xorg-wm
pkgver=0.0.1
pkgrel=1 # release of this package version
pkgdesc="Personal Xorg WM setup"
url="http://gitea.local:3000/root/Arch-Packages"
arch=('any')
license=('MIT')
groups=('desktop-pc')
source ../resolve_dependencies.sh
depends=($(resolve_dependencies "xorg-wm.depends"))
depends+=( 'personal-xorg' )

package() {
    return 0
}

